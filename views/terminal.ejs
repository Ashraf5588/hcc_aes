<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Terminal Selection</title>
  <link rel="stylesheet" href="/nav.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lexend:wght@100..900&display=swap" rel="stylesheet">
  <!-- Add Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    :root {
      --primary: #0ea5e9;  /* skyblue */
      --primary-dark: #0284c7;  /* darker skyblue */
      --secondary: #8b5a2b;  /* brown */
      --secondary-dark: #6b4423;  /* darker brown */
      --light: #f8fafc;
      --dark: #1e293b;
      --accent: #a16207;  /* amber brown for accent */
      --gray: #64748b;
      --success: #22c55e;
      --error: #ef4444;
      --warning: #f59e0b;
    }
    
    body {
      background-color: #f8fafc;
      font-family: 'Lexend', sans-serif;
      color: var(--dark);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }
    
    header {
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 80%);
      margin-bottom: 2rem;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
      border-bottom-left-radius: 20px;
      border-bottom-right-radius: 20px;
      color: white;
      padding-bottom: 1rem;
    }
    
    .hero-section {
      padding: 2rem 1rem;
      text-align: center;
    }
    
    #firstheading {
      font-size: 2.5rem;
      font-weight: 700;
      margin-top: 0;
      color: white;
    }
    
    #firstspan {
      color: var(--accent);
      font-weight: 800;
    }
    
    .subheading {
      color: rgba(255, 255, 255, 0.85);
      font-size: 1.2rem;
      display: block;
      margin-top: 0.5rem;
    }
      .terminalcontainer {
      max-width: 1200px;
      margin: 0 auto 2rem;
      padding: 0 1rem;
    }
    
    .terminal-card {
      transition: all 0.3s ease;
      border-radius: 12px;
      overflow: hidden;
      border-bottom: 3px solid var(--primary) !important;
    }
    
    .terminal-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1) !important;
    }
    
    .terminal-icon {
      height: 80px;
      width: 80px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
      color: white;
    }
    
    .custom-terminal .terminal-icon {
      background: linear-gradient(135deg, var(--secondary) 0%, var(--secondary-dark) 100%);
    }
    
    .compare-terminal .terminal-icon {
      background: linear-gradient(135deg, var(--secondary) 0%, var(--secondary-dark) 100%);
    }
    
    .btn-primary {
      background: var(--primary);
      border-color: var(--primary);
    }
    
    .btn-primary:hover {
      background: var(--primary-dark);
      border-color: var(--primary-dark);
    }
    
    .btn-secondary {
      background: var(--secondary);
      border-color: var(--secondary);
    }
    
    .btn-secondary:hover {
      background: var(--secondary-dark);
      border-color: var(--secondary-dark);
    }
  </style>
</head>
<body>
  <header>
    <%- include('./partials/nav',{currentPage:'teacher'}) %>
    <div class="hero-section">
      <h1 id="firstheading">Terminal <span id="firstspan">Examinations</span></h1>
      <span class="subheading">Select a terminal examination period to continue</span>
    </div>
  </header>

  <%- include('partials/sidenav', { 
      subjects: subjects, 
      studentClassdata: studentClassdata,
      terminals: terminals 
  }) %>

  <div class="terminalcontainer">
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">

      <% terminalList.forEach((terminal)=>{%>
      
      <div class="col">
        <a href="<%=section %>/<%= terminal.terminal.toUpperCase() %>" class="text-decoration-none">
          <div class="card h-100 border-0 shadow-sm terminal-card">
            <div class="card-body text-center p-4">
              <div class="terminal-icon mb-3">
                <i class="fa-solid fa-calendar-check fa-3x"></i>
              </div>
              <h3 class="card-title"><%= terminal.terminal.toUpperCase() %> TERMINAL </h3>
              <p class="card-text text-muted"><%= terminal.terminal.toUpperCase() %> TERMINAL examination results and analysis</p>
            </div>
            <div class="card-footer bg-transparent border-0 text-center p-3">
              <span class="btn btn-sm btn-primary">Select <i class="fas fa-arrow-right ms-1"></i></span>
            </div>
          </div>
        </a>
      </div>
      <%})%>
      
     
      
     
     
      
      <!-- <div class="col" style="display:<%= controller==='forms'?'none':'block'%>">
        <a href="<%=section %>/termwise/status" class="text-decoration-none">
          <div class="card h-100 border-0 shadow-sm terminal-card compare-terminal">
            <div class="card-body text-center p-4">
              <div class="terminal-icon mb-3">
                <i class="fa-solid fa-chart-line fa-3x"></i>
              </div>
              <h3 class="card-title">Compare Terminals</h3>
              <p class="card-text text-muted">Compare results across different terminal periods</p>
            </div>
            <div class="card-footer bg-transparent border-0 text-center p-3">
              <span class="btn btn-sm btn-secondary">Compare <i class="fas fa-chart-bar ms-1"></i></span>
            </div>
          </div>
        </a> -->
      </div> 
    </div>
  </div>

  <!-- Bootstrap JS and Font Awesome -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://kit.fontawesome.com/368eb67b69.js" crossorigin="anonymous"></script>
</body>
</html>